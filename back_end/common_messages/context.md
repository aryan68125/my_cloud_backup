# common_messages/

| File | Purpose |
|---|---|
| `error_messages.py` | `ErrorMessages` enum — all human-readable error strings used as HTTP exception detail values. |
| `success_messages.py` | `SuccessMessages` enum — all human-readable success strings returned in `MessageOut` response bodies. |
| `warning_messages.py` | `WarningMessages` enum — warning strings for future use in warning-level responses. |
| `context.md` | This file. |
